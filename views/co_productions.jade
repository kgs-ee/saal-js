extends layout

block vars

block content

    .container-fluid
        .row.page-margin

            section.col-md-12
                .programme.row
                    .col-xs-12.col-md-12
                        h2= t('nav.coproductions')
                    
                    .grid-row
                        each performance in performances
                            a.col-xs-12.col-sm-4.col-md-3.col-lg-2.co-production(href='/' + lang + '/performance/' + op.get(performance,'id'))
                                img.img-responsive(src=op.get(performance, ['photos', 0, 'medium', 'file']))
                                p= op.get(performance, ['artist'], '')
                                p= op.get(performance, [lang+'-name'], '')
