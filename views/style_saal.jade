style.
    /**
    ** SAAL COLOR SCHEME
    */
    
    #front-carousel,
    .event-banner .item {
        background-color: #{SAAL.secondary_color};
    }

    a:hover {
        color: #{SAAL.secondary_color};
    }

    .select-cat .active {
        color: #{SAAL.secondary_color};
    }

    .select-cat label:hover {
        color:#{SAAL.secondary_color};
    }

    li.event,
    .highlight.event {
        background-color: #{SAAL.main_color};
    }

    li.tour,
    .highlight.tour {
        background-color: #28DF24;
    }

    li.tour {
    margin-bottom: 0;
    }

    li.residency,
    .highlight.residency {
        background-color: #{SAAL.secondary_color};
    }

    .navbar-nav > .active > a,
    .main-nav a:hover,
    .legend li.event:before {
        color: #{SAAL.main_color} !important;
    }

    .legend li.residency:before {
        color: #{SAAL.secondary_color};
    }

    .legend li.tour:before {
        color: #28DF24;
    }

    .legend li.event,
    .legend li.tour,
    .legend li.residency {
        background-color: transparent;
    }

    .today {
        color: #{SAAL.main_color};
    }

    .btn,
    .btn-primary {
        background-color: #{SAAL.secondary_color};
        border-color: #{SAAL.secondary_color};
    }

    .btn:hover,
    .btn-primary:hover {
        color: #{SAAL.secondary_color};
        border-color: #{SAAL.secondary_color};
    }

    .btn:focus,
    .btn-primary:focus {
        background-color: #{SAAL.secondary_color};
        border-color: #{SAAL.secondary_color};
    }

    .open>.btn-default.dropdown-toggle {
        background-color: #{SAAL.secondary_color};
        border-color: #{SAAL.secondary_color};
    }

    .btn-default {
        color: #{SAAL.secondary_color};
        border-color: #{SAAL.secondary_color};
    }

    .btn-default:hover {
        background-color: #{SAAL.secondary_color};
        border-color: #{SAAL.secondary_color};
    }

    .social a {
        color: #{SAAL.main_color} !important;
    }

    .social a:hover {
        color: #{SAAL.secondary_color};
    }

    .volunteer a {
        background-color: #{SAAL.main_color};
        color: #{SAAL.secondary_color};
    }

    .volunteer a:hover {
        border: 2px solid #{SAAL.secondary_color};
        color: #{SAAL.secondary_color};
    }

    .panel h3 a:focus,
    .panel .select-cat label a:focus,
    .select-cat .panel label a:focus {
        color: #{SAAL.secondary_color};
    }

    #mailchimp-sign-up input[type="text"] {
        border: 2px solid #{SAAL.secondary_color};
        color: #{SAAL.secondary_color};
    }

    #mailchimp-sign-up input[type="submit"] {
        color: #{SAAL.secondary_color};
    }

    .matched {
        color: red;
    }

    .pager li > a,
    .pager li > span {
        background-color: #{SAAL.secondary_color};
        border: 1px solid #{SAAL.secondary_color};
        color: #fff;
    }

    .pager li > a:hover,
    .pager li > span:hover {
        background-color: #fff;
        border: 1px solid #{SAAL.secondary_color};
        color: #{SAAL.secondary_color};
    }

    footer {
        background-color: #{SAAL.secondary_color};
    }

    .site-search input {
        border: 2px solid #{SAAL.secondary_color};
        color: #{SAAL.secondary_color};
    }

    .site-search button{
        color: #{SAAL.secondary_color};
    }
